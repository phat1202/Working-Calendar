@model Calendar.Models.Holiday

@{
    ViewData["Title"] = "AddHoliday";
}

<h1>AddHoliday</h1>

<h4>Holiday</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddHoliday">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="holidays" class="control-label"></label>
                <input asp-for="holidays" class="form-control" />
                <span asp-validation-for="holidays" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
